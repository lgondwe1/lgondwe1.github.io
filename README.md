The website was deployed using GitHub Pages, a free hosting service that allows static websites to be publicly accessible. The project files were uploaded to a GitHub repository and linked to GitHub Pages through the repository settings. Once deployed, the website became accessible via a public URL. HTTPS was automatically enabled, ensuring secure communication using TLS encryption. Updates to the website can be deployed by committing changes to the repository, which are then automatically reflected on the live site.
